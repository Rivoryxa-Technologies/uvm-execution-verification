# Contributing

Keep every claim tied to executed UVM evidence. Preserve immutable upstream
revisions, exact scoreboard classification, process timeouts, retained logs,
and license provenance. Do not replace class execution with lint or accept an
arbitrary failure as the deliberate mismatch result.

Run `make test` before submitting changes. Generated `artifacts/` are local and
must not be committed. Contributions are accepted under the MIT License.
